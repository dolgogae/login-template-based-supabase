# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This is a new, empty project intended to be a login template. No framework or dependencies have been set up yet.

## Permissions

`.claude/settings.local.json` restricts tool permissions to only allow `Bash(test:*)` commands. Update this file as the project grows and additional commands are needed.
